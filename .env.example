# ENVIRONMENT options: qqbot, onebotv11, onebotv11-wsRev
ENVIRONMENT=onebotv11-wsRev
DRIVER=~fastapi+~httpx+~websockets
LOCALSTORE_USE_CWD=true

# APScheduler 配置（定时任务）
# APSCHEDULER_AUTOSTART=true  # 是否自动启动定时任务，默认 true

# 自动统计调试配置（使用 NoneBot 配置格式，统一小写）
# auto_stats_debug=true        # 调试模式：不发送图片到群，只保存到 data 目录
# auto_stats_run_on_startup=true  # 启动时立即执行一次统计（调试用）